let s=new String("Hello")
let a=new String("Hello")
let b="Hello"
let c="Hello"
console.log(s+" "+a)
if(a===c)
{
    console.log("Same")
}
else{
    console.log("Differ")
}
//(obj==string) having same contain then it result is true 
//it compare by contain and obj is converted into string
//(obj===string) having same contain then it result is false  it compare by address
//(obj==obj) having same contain then it result is false  it compare by address
//(obj===obj) having same contain then it result is false  it compare by address
